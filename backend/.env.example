# LM Studio (локальный сервер) — API-ключ может быть любой строкой; обязателен (не оставляйте пустым)
OPENAI_API_KEY=lm-studio
OPENAI_BASE_URL=http://localhost:1234/v1

# Имя модели должно совпадать с тем, что загружено в LM Studio
# (можно посмотреть в разделе "Local Server" → выбранная модель)
OPENAI_MODEL=qwen/qwen3-4b-2507

# Только для /validate_range — точный id из LM Studio. Пусто = OPENAI_MODEL.
OPENAI_VALIDATE_MODEL=qwen/qwen3-4b-2507

# Сколько страниц PDF объединять в один запрос к ИИ (по умолчанию 1).
# Для больших контекстных моделей можно увеличить (2-4).
PDF_PAGES_PER_CHUNK=1

# Максимальный размер файла для обработки (МБ, по умолчанию 100).
MAX_FILE_SIZE_MB=100

# Через сколько часов удалять файлы результатов из временной директории (по умолчанию 24).
RESULT_TTL_HOURS=24

# Разрешённые CORS-источники (через запятую). Только для разработки —
# в production браузер работает через nginx с тем же origin.
# CORS_ORIGINS=http://localhost:5173,http://localhost:80

