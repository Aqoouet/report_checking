# TODO

- [ ] **Global status in GUI** — Show how many checks are running now (concurrent / in-progress jobs), so users can see service load at a glance.
- [ ] **Refresh button** — Add a control to refresh the global status (and related runtime info) without reloading the whole page.

## Согласовано с Вячеславом (фидбек по первому использованию)

- [x] **Баг: удаление строки крестиком** — Кнопка × в форме с вводом пути не удаляет строку. Исправить.
- [ ] **Подсказка по формату пути** — Добавить кнопку «?» рядом с полем пути с всплывающей подсказкой и примерами для `P:\`, `U:\` и `\\e0-filer03\`. Удалить полностью всплывающие подсказки и заменить на кнопочку с вопросиком. Добавить пути под `\\e0-filer03\` они смонтированы по разному так что корень пропиать просто не получится.
- [ ] **Загрузка промптов из файла** — Завести папку с файлами промптов на сервере (по одному на проект). Добавить в GUI выбор файла промпта с автоподстановкой текста в поле промпта.
- [ ] **Убрать «контекст ~262 144 ток.» из GUI** — Параметр не влияет на работу (chunk зафиксирован отдельно), только вводит в заблуждение.
- [ ] **Параллельная проверка разделов** — Включить параллельный режим (инфраструктура поддерживает до 12 разделов одновременно), постепенно наращивать параллелизм. Рассмотреть создание отдельного контейнера. Учесть очереди: чтобы сервер не перегружался в этом случае лучше создать в GUI что ваша проверка в очереди под номером ...
- [ ] **Фоновая очередь + закрытие вкладки** — Реализовать фоновую обработку: задачи ставятся в очередь, вкладку можно закрыть. Результаты сохраняются в единую папку на сервере. Тогда нужно может быть сделать иснтрукцмю что файл содастся в любом случае даже если проверка не была успешной. И также реализовать это в коде.
- [ ] **Входной промпт в выходных файлах** — Сохранять полный текст промпта в итоговые файлы с результатами проверки. В имени файла с отчетом об ошибках отразить название файла и время когда была запущена проверка.
- [ ] **Убрать артефакт «проверено 1 критерий»** — Остаток старой логики трёх последовательных проверок. Убрать или поправить счётчик.
- [ ] **Шаг обобщения (сводка ошибок)** — Добавить финальный шаг: нейросеть делает сводку часто повторяющихся замечаний по всему отчёту. Добавить отдельное поле промпта для этого шага в GUI.
- [ ] **Валидационный промпт для фильтрации шума** — Добавить шаг предварительной валидации замечаний (отдельный запрос к модели), чтобы отсеять ложные срабатывания. Добавить отдельное поле промпта в GUI.
- [ ] **Справка по ограничениям сервиса** — Описать что проверяется (только текст), что не проверяется (рисунки, таблицы-изображения) и что остаётся на нормоконтроле.
- [ ] **Добавить автотесты** — При температуре 0 прогнать отчет и проверить результат с заранее известным.
- [ ] **Добавить регулировку детерминируемости** — При запросах через GUI авто параметры, через API регулировка при запросе.