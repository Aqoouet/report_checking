# TODO

- [ ] **Global status in GUI** — Show how many checks are running now (concurrent / in-progress jobs), so users can see service load at a glance.
- [ ] **Refresh button** — Add a control to refresh the global status (and related runtime info) without reloading the whole page.
