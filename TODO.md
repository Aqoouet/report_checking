# TODO

Items ordered from simplest to hardest. Implement sequentially where dependencies exist.

---



## 6. Упростить UX запуска проверки и работы с задачами
После нажатия «check report» на первой странице показывать только короткий статус: «проверка запущена» или сообщение об ошибке.
После нажатия `OK` возвращать пользователя на первую страницу:
- если запуск не удался, поля формы остаются заполненными ранее введёнными данными;
- если запуск успешен, создаётся job и пользователь возвращается в основной экран.

Проверка должна продолжаться независимо от вкладки браузера (вкладку можно закрыть без отмены job).
При повторном открытии GUI пользователь видит все `jobs` (для всех пользователей), без разделения по пользователям.
**Сложность:** высоко — обновление UX-сценария и сохранение/восстановление состояния задач.

## 7. Хранение результатов проверок
После завершения проверки сохранять:
- Markdown-файл с найденными ошибками;
- полный Markdown-файл отчёта.

Сохранять результаты в общий каталог `/filer/users/rymax1e/CHECK_RESULTS` (путь вынести в параметр конфигурации).
Добавить автоочистку файлов старше 30 дней.
Обновить инструкции в GUI с описанием, где искать результаты и как отслеживать статус.
**Сложность:** средне-высоко — файловое хранение, конфигурация пути, фоновая очистка, обновление текстов в интерфейсе.




## 12. Параллельная проверка разделов
Включить параллельный режим проверки:
- параллелизм по пользователям;
- параллелизм по подразделам внутри одного отчёта;
- параметризуемый лимит одновременных проверок (базово до 4) — это максимальное число запросов к одному endpoint LMS API.

Поддержать одновременные запросы к нескольким LLM-контейнерам (в том числе несколько запросов к одному endpoint) с контролем перегрузки каждого endpoint (лимиты/очереди/блокировка превышения).

Добавить в GUI раздел `Active jobs` с кнопкой обновления:
- показывать, какие проверки выполняются и какие стоят в очереди;
- по умолчанию: одна проверка в работе, остальные в очереди (если лимит = 1);
- отображать упорядоченную таблицу: имя файла и время постановки в очередь;
- без push-уведомлений: пользователь сам обновляет состояние.

Минимальный набор параметров конфигурации:
- максимальное число запросов к одному AI-инстансу;
- максимальное число одновременно проверяемых подразделов;
- максимальное число активных `jobs` в системе.

**Зависит от:** пункта 6 (единая модель job-статусов в интерфейсе).
**Сложность:** высоко — изменение архитектуры бэкенда, управление конкурентностью и очередями, обновление GUI.



## 8. Загрузка промптов из файла
Завести папку с файлами промптов на сервере (по одному на проект). Добавить в GUI выбор файла промпта с автоподстановкой текста в поле промпта.
**Сложность:** средне-высоко — управление файлами на сервере + эндпоинт + фронтенд.


## 9. Валидационный промпт для фильтрации шума
Добавить шаг предварительной валидации замечаний (отдельный запрос к модели), чтобы отсеять ложные срабатывания. Добавить отдельное поле промпта в GUI.
**Сложность:** средне-высоко — новый шаг в пайплайне обработки + GUI.

## 10. Шаг обобщения (сводка ошибок)
Добавить финальный шаг: нейросеть делает сводку часто повторяющихся замечаний по всему отчёту. Добавить отдельное поле промпта для этого шага в GUI.
**Зависит от:** пункта 9 (логично добавлять обобщение после фильтрации шума).
**Сложность:** средне-высоко — новый финальный шаг в пайплайне + GUI.


## 11. Добавить автотесты
При температуре 0 прогнать отчёт и проверить результат с заранее известным.
**Зависит от:** пункта 5 (нужна регулировка температуры).
**Сложность:** высоко — тестовая инфраструктура, фикстуры, CI.

---

## Выполнено

- [x] **Подсказка по формату пути** — Кнопка «?» рядом с полями пути и диапазона заменила hover-подсказки. Всплывающая панель показывает все пути из `path_mapping.json` (PathField) и примеры формата диапазона (RangeField).
- [x] **Баг: удаление строки крестиком** — Кнопка × в форме с вводом пути не удаляла строку. Исправлено.
- [x] **Убрать «контекст ~262 144 ток.» из GUI** — Параметр не влияет на работу (chunk зафиксирован отдельно), только вводил в заблуждение. Убрано.
- [x] **Убрать артефакт «проверено 1 критерий»** — Остаток старой логики трёх последовательных проверок. Убран счётчик критериев из экрана результатов.
- [x] **Справка по ограничениям сервиса** — Кнопка «?» рядом с заголовком заменила статичный subtitle. Всплывающая панель описывает что проверяется (текст), что не проверяется (рисунки) и что остаётся на нормоконтроле (шрифты, стили, параметры Word).
- [x] **Входной промпт в выходных файлах** — Полный текст промпта сохраняется в файл отчёта. Имя скачиваемого файла содержит название документа и время запуска проверки (`stem_YYYYMMDD_HHMMSS_errors.txt`).
- [x] **Регулировка детерминируемости (temperature)** — Параметр `temperature` прокинут через весь стек: `/check` API принимает его как Form-поле (пусто = авто), значение доходит до вызова модели. В GUI — необязательное поле в «Информация о модели» (пусто = авто). `AI_TEMPERATURE` env var задаёт серверное значение по умолчанию.
