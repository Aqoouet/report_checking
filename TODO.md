# TODO

Items ordered from simplest to hardest. Implement sequentially where dependencies exist.

---

## 1. Убрать артефакт «проверено 1 критерий»
Остаток старой логики трёх последовательных проверок. Убрать или поправить счётчик.
**Сложность:** тривиально — только фронтенд.

## 2. Справка по ограничениям сервиса
Описать что проверяется (только текст), что не проверяется (рисунки, таблицы-изображения) и что остаётся на нормоконтроле.
**Сложность:** тривиально — статичный текст/модал в GUI.

## 3. Подсказка по формату пути
Добавить кнопку «?» рядом с полем пути с всплывающей подсказкой и примерами для `P:\`, `U:\` и `\\e0-filer03\`. Удалить полностью всплывающие подсказки и заменить на кнопочку с вопросиком. Добавить пути под `\\e0-filer03\` — они смонтированы по-разному, так что корень прописать просто не получится.
**Сложность:** просто — только фронтенд.

## 4. Входной промпт в выходных файлах
Сохранять полный текст промпта в итоговые файлы с результатами проверки. В имени файла с отчётом об ошибках отразить название файла и время когда была запущена проверка.
**Сложность:** просто — изменение формата вывода на бэкенде.

## 5. Добавить регулировку детерминируемости
При запросах через GUI авто параметры, через API — регулировка при запросе.
**Сложность:** средне — добавить параметр temperature в API и опционально в GUI.

## 6. Global status in GUI
Show how many checks are running now (concurrent / in-progress jobs), so users can see service load at a glance.
**Сложность:** средне — новый эндпоинт на бэкенде + отображение в GUI.

## 7. Refresh button
Add a control to refresh the global status (and related runtime info) without reloading the whole page.
**Зависит от:** пункта 6 (кнопка обновляет именно этот статус).
**Сложность:** тривиально после пункта 6 — только фронтенд.

## 8. Загрузка промптов из файла
Завести папку с файлами промптов на сервере (по одному на проект). Добавить в GUI выбор файла промпта с автоподстановкой текста в поле промпта.
**Сложность:** средне-высоко — управление файлами на сервере + эндпоинт + фронтенд.

## 9. Валидационный промпт для фильтрации шума
Добавить шаг предварительной валидации замечаний (отдельный запрос к модели), чтобы отсеять ложные срабатывания. Добавить отдельное поле промпта в GUI.
**Сложность:** средне-высоко — новый шаг в пайплайне обработки + GUI.

## 10. Шаг обобщения (сводка ошибок)
Добавить финальный шаг: нейросеть делает сводку часто повторяющихся замечаний по всему отчёту. Добавить отдельное поле промпта для этого шага в GUI.
**Зависит от:** пункта 9 (логично добавлять обобщение после фильтрации шума).
**Сложность:** средне-высоко — новый финальный шаг в пайплайне + GUI.

## 11. Добавить автотесты
При температуре 0 прогнать отчёт и проверить результат с заранее известным.
**Зависит от:** пункта 5 (нужна регулировка температуры).
**Сложность:** высоко — тестовая инфраструктура, фикстуры, CI.

## 12. Параллельная проверка разделов
Включить параллельный режим (инфраструктура поддерживает до 12 разделов одновременно), постепенно наращивать параллелизм. Рассмотреть создание отдельного контейнера. Учесть очереди: чтобы сервер не перегружался — показывать в GUI позицию в очереди.
**Сложность:** высоко — изменение архитектуры бэкенда, управление очередью, обновление GUI.

## 13. Фоновая очередь + закрытие вкладки
Реализовать фоновую обработку: задачи ставятся в очередь, вкладку можно закрыть. Результаты сохраняются в единую папку на сервере. Файл создаётся в любом случае, даже если проверка не была успешной.
**Зависит от:** пункта 12 (фоновая очередь строится поверх параллельной обработки).
**Сложность:** очень высоко — персистентная очередь, хранение состояния, уведомления.

---

## Выполнено

- [x] **Баг: удаление строки крестиком** — Кнопка × в форме с вводом пути не удаляла строку. Исправлено.
- [x] **Убрать «контекст ~262 144 ток.» из GUI** — Параметр не влияет на работу (chunk зафиксирован отдельно), только вводил в заблуждение. Убрано.
