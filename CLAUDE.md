# Claude Project Instructions

Before searching the codebase, read `INDEX.md` first.

Use `INDEX.md` as the primary map of repository files to quickly locate the relevant files for the task.

In the end of each modificaion af all files you  need to commit, push and perform on remote server using ssh: ~/report_checking/rebuild-back-front.sh to pull and rebuild backend and front end. ssh command is ssh rymax1e-wg.

If you are fixing point from TODO.md mark it finished at the end.
